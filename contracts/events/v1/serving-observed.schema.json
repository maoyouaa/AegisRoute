{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aegisroute.dev/contracts/events/v1/serving-observed.schema.json",
  "title": "ServingObservedV1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "eventId", "requestId", "rolloutId", "routeVersion", "deploymentId", "candidate", "outcome", "statusCode", "latencyMs", "observedAt"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "eventId": { "type": "string", "format": "uuid" },
    "requestId": { "type": "string", "minLength": 1 },
    "rolloutId": { "type": "string", "format": "uuid" },
    "routeVersion": { "type": "integer", "minimum": 1 },
    "deploymentId": { "type": "string", "minLength": 1 },
    "candidate": { "type": "boolean" },
    "outcome": { "enum": ["SUCCESS", "HTTP_ERROR", "TIMEOUT", "CANCELLED", "STREAM_ERROR"] },
    "statusCode": { "type": "integer", "minimum": 0, "maximum": 599 },
    "latencyMs": { "type": "integer", "minimum": 0 },
    "observedAt": { "type": "string", "format": "date-time" }
  }
}
